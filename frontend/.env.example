# 감자마켓 frontend 서버 전용 환경 변수 예시.
# 실제 값은 frontend/.env.local 에 넣고 추적하지 않는다.

# Next.js rewrite가 /api/auth/* 를 전달할 FastAPI origin.
# NEXT_PUBLIC 접두사를 붙이지 않으며 끝에 / 를 붙이지 않는다.
# 개발: http://127.0.0.1:8000 / 운영: 실제 FastAPI HTTPS origin
BACKEND_API_ORIGIN=http://127.0.0.1:8000
